```js
js不同于其他的编程语言，js允许我们在不是Boolean的值之间使用逻辑运算符 当js编译器处理一个表达式时，它先看第一个值，当第一个值是真的时候，它就看第二个值，因为第二个值不是布尔值，所以js编译器尝试转换成 类真 或 类假 值，空字符串是被视为类假的，一个非空的字符串是类真的 在这种情况下 js编译器返回第二个类真值 
	数字0被视为类假 结果会返回最后一个类真值
true && "hahahah";
"hahahah"
true && 'hi' && 999
999


React中的一段：
/*如果是条件性的返回值，可以使用逻辑运算符。首先给定一个条件，然后把条件为真时希望给的字符串写在&&后面,可以是字符串也可以是jsx表达式*/
        <ul>
          {this.state.tags.length == 0 && "Please give a tag"} 
          {this.renderTags()}
        </ul>
```

